import '../components/kwc-auth-landing.js';

suite('LandingPage', () => {

    test('A new user can be directed to the page they can signup with Kano', () => {
        assert.equal
    })

    test('return error 404 if signup page not found', () => {
        assert.equal
    })

    test('An exsiting user can be directed to the page they can login with kano', () =>{
        assert.equal
    })

    test('return error 404 if login page not found', () => {
        assert.equal
    })
    
});